# 工艺与特殊工艺迁移说明

本文档用于在原作者后续更新测试服助手脚本后，重新迁移“特殊工艺也进入工艺词缀选择器”的本地补丁。

当前参考文件：`idlepoe-assistant.user.js`

当前参考版本：`2.24.2.6`

## 版本迁移总览

本轮迁移基于原作者的 2.23.9、2.24、2.24.1、2.24.2 更新，当前本地补丁版本为 `2.24.2.6`。

涉及的提交：

- `e79ef79`：以 2.23.9 为基准迁移工艺词缀特殊工艺补丁
- `6e854a1`：修复装备类型掩码查找
- `ae788ac`：迁移 2.24
- `107fd65`：迁移 2.24.1
- `93a227c`：迁移 2.24.2
- `44b506e`：增加工艺台和花园工艺的珠宝分类
- `29d1b93`：修复暗金打造花园工艺的珠宝选项过滤
- `afeb90e`：修复珠宝花园工艺列表缓存
- `de945db`：修复自定义打造珠宝装备类型推断
- `4228003`：允许按已保存的花园工艺 ID 执行

后续原作者更新脚本时，迁移顺序建议保持：

1. 先以原作者新版本替换基础脚本。
2. 再迁移本文件中的“工艺词缀特殊工艺”补丁。
3. 再迁移“珠宝工艺分类与执行”补丁。
4. 最后迁移破裂词缀 Roll 排除逻辑。
5. 执行语法检查和接口验证。

## 珠宝工艺迁移

### 问题表现

珠宝在以下位置可能出现无工艺或执行失败：

- `打造装备 -> 自定义打造 -> 花园工艺 -> 珠宝`
- 暗金打造中的花园工艺选择
- 已选择珠宝花园工艺后，运行时报：

```text
请先选择适用于该装备的花园工艺。
```

### 根因

珠宝与项链、戒指、腰带使用不同的装备类型掩码：

```js
EQUIPMENT_TYPE_MASKS.jewels
EQUIPMENT_TYPE_MASKS.amulets
EQUIPMENT_TYPE_MASKS.rings
EQUIPMENT_TYPE_MASKS.belts
```

原作者接口当前不一定接受 `EQUIPMENT_TYPE_MASKS.jewels` 作为
`/api/equipment/garden/list/{equipmentType}` 的独立查询类型。若直接请求珠宝类型，可能返回：

```json
{
  "success": false,
  "message": "无效的装备类型"
}
```

因此不能只增加一个 `jewels` 启动请求。需要复用已经成功加载的 `jewelry`
花园工艺缓存，并保留珠宝自己的分类掩码用于装备类型判断。

### 必须保留的代码锚点

- `EQUIPMENT_TYPE_MASKS.jewels`
- `CRAFT_BENCH_CATEGORY_OPTIONS` 中的 `jewels`
- `GARDEN_CRAFT_CATEGORY_OPTIONS` 中的 `jewels`
- `GARDEN_CRAFT_STARTUP_REQUESTS`
- `getGardenCraftCategoryList`
- `getGardenCraftOptionsByCategory`
- `getGardenCraftsByEquipmentType`
- `getContinuousGardenCraftEquipmentTypeMask`
- `applyGardenCraftForEquipmentType`

### 珠宝缓存迁移规则

启动请求应加载通用花园分类和首饰分类，不要强制请求后端不支持的珠宝列表：

```js
const GARDEN_CRAFT_STARTUP_REQUESTS = [
  { categoryValue: 'weapons', equipmentType: 1n },
  { categoryValue: 'armours', equipmentType: 134217728n },
  { categoryValue: 'jewelry', equipmentType: EQUIPMENT_TYPE_MASKS.amulets },
];
```

首饰列表加载成功后，为珠宝分类建立兜底缓存：

```js
if (!state.gardenCraft.byCategory.jewels?.length
  && state.gardenCraft.byCategory.jewelry?.length) {
  state.gardenCraft.byCategory.jewels = state.gardenCraft.byCategory.jewelry;
  state.gardenCraft.byEquipmentType[
    normalizeEquipmentTypeCacheKey(EQUIPMENT_TYPE_MASKS.jewels)
  ] = state.gardenCraft.byCategory.jewelry;
}
```

读取分类列表时，`jewels` 应优先使用自己的缓存，没有内容时回退到
`jewelry`：

```js
const getGardenCraftCategoryList = (categoryValue) => {
  const category = getGardenCraftCategory(categoryValue);
  if (category.value === 'jewels') {
    return state.gardenCraft.byCategory.jewels?.length
      ? state.gardenCraft.byCategory.jewels
      : (state.gardenCraft.byCategory.jewelry || []);
  }
  return state.gardenCraft.byCategory[category.value] || [];
};
```

### 珠宝工艺适用性

珠宝花园工艺不能继续被只读 `kind === 'enchantment'` 的逻辑限制。
`ReforgeCritical` 就是实际使用的非附魔工艺方法。

选择器和校验应使用：

```js
getGardenCraftsByEquipmentType(equipmentTypeMask)
```

不要使用：

```js
getGardenCraftsByEquipmentType(equipmentTypeMask, true)
```

对珠宝装备，非附魔类花园工艺需要允许继续匹配，由后端最终判断是否可执行：

```js
if ((mask & EQUIPMENT_TYPE_MASKS.jewels) !== 0n
  && craft?.kind !== 'enchantment') {
  return true;
}
```

### 自定义打造执行链路

`getCraftTargetEquipmentTypeMask()` 为了避免普通工艺台误筛珠宝，可能对珠宝返回
`0n`。自定义打造的花园工艺不能直接复用这个结果。

应优先读取装备对象中的实际类型；读取不到时，使用当前步骤保存的
`gardenCraftCategory`：

```js
const getContinuousGardenCraftEquipmentTypeMask = (equipment, step) => {
  const equipmentMask = parseEquipmentTypeMask(
    getEquipmentTypeValueForFilter(equipment),
  );
  if (equipmentMask > 0n) return equipmentMask;
  return getGardenCraftCategory(step?.gardenCraftCategory).mask
    || getCraftTargetEquipmentTypeMask(state.ui.equipmentTypeSelect?.value);
};
```

### 工艺 ID 兜底执行

即使列表缓存没有命中，用户已经选择并保存的工艺 key 也不应在前端提前失败。
`applyGardenCraftForEquipmentType` 应允许按保存的 key 直接调用：

```js
const craftId = String(parsedSelection.gardenCraftKey || normalizedGardenCraftKey).trim();
const craft = getGardenCraftsByEquipmentType(equipmentTypeMask)
  .find((item) => item.key === normalizedGardenCraftKey || item.key === craftId)
  || findLoadedGardenCraftByKey(normalizedGardenCraftKey)
  || findLoadedGardenCraftByKey(craftId)
  || { key: normalizedGardenCraftKey, craftId, label: craftId };
```

这样后端可以返回真实的适用性错误，而不是前端统一报：

```text
请先选择适用于该装备的花园工艺。
```

### 接口验证记录

珠宝定向重铸接口验证成功：

```json
{
  "equipmentId": "6a6ec5ca426b4dc497e8fb82",
  "craftId": "ReforgeCritical"
}
```

接口返回 `success: true`，装备类型为钴蓝珠宝，并生成了暴击相关词缀。
这说明后端支持珠宝执行 `ReforgeCritical`，前端迁移重点是列表缓存、装备类型推断和
工艺 ID 传递。

## 迁移目标

本补丁解决的问题是：

- 原作者 `craft/list` 接口里新增了不带 `affix` 结构的特殊工艺。
- 这类工艺虽然能直接通过 `/api/craft` 调用，但原有脚本只把带 `affix` 的工艺加入工艺词缀缓存。
- 结果就是网页里明明能用的新工艺，在助手的“工艺词缀”选择器中完全看不到。

当前已接入的特殊工艺：

- `随机重掷稀有物品上的 3 条词缀`
- `随机重掷稀有物品上的 1 条词缀`

这两个工艺应按“全类型通用工艺”处理，效果上类似：

- `移除工艺词缀`

也就是：

- 不依赖普通 `affix.type = 1 / 2` 前后缀结构
- 需要进入工艺词缀缓存
- 需要在所有装备类型的工艺词缀选择器中可见

## 接口依据

本次迁移不是猜测，是按网页真实接口记录确认的。

`/api/craft/list` 中这两个工艺的关键结构如下：

```json
{
  "craftId": 18301,
  "text": "随机重掷稀有物品上的 3 条词缀",
  "action": { "type": "rerollRandomModifiers", "count": 3 },
  "requiredRarity": 3
}
```

```json
{
  "craftId": 18302,
  "text": "随机重掷稀有物品上的 1 条词缀",
  "action": { "type": "rerollRandomModifiers", "count": 1 },
  "requiredRarity": 3
}
```

关键点：

- 它们只有 `text` 和 `action`
- 没有普通工艺那种 `affix`
- 文案是 `随机重掷`，不是 `随机重铸`
- 文案中数字两侧有空格：`3 条` / `1 条`

如果下次迁移时把关键词写成：

- `随机重铸`
- `随机重掷稀有物品上的3条词缀`

这种不带空格的精确匹配，很容易再次漏掉。

## 主要代码锚点

下次迁移时优先搜索这些函数：

- `isUniversalCraftBenchCraft`
- `formatCraftBenchLabel`
- `fetchCraftBenchList`
- `filterCraftBenchAffixesForPicker`
- `isCraftBenchForMask`

推荐检索命令：

```powershell
rg -n "isUniversalCraftBenchCraft|formatCraftBenchLabel|fetchCraftBenchList|filterCraftBenchAffixesForPicker|随机重掷稀有物品上的" "idlepoe-assistant.user.js"
```

## 迁移要点

### 1. 增加特殊工艺识别函数

需要保留一个专门识别“无 affix 特殊工艺”的函数。

当前实现思路：

```js
const isUniversalCraftBenchCraft = (craft) => {
  const text = [
    craft?.label,
    craft?.text,
    craft?.name,
    craft?.description,
    craft?.effectText,
    craft?.affix?.name,
    craft?.affix?.displayName,
    craft?.searchText,
  ].map((value) => String(value || '').trim()).filter(Boolean).join(' ');
  return /随机重掷稀有物品上的\s*(?:1|3)\s*条词缀/.test(text);
};
```

迁移要求：

- 必须使用 `随机重掷`
- 必须允许数字前后空格
- 不要只查 `craft.affix.name`
- 要同时兼容 `text / label / searchText` 等多个来源

### 2. 工艺列表缓存阶段不要把它过滤掉

原逻辑只保留：

```js
craft?.affix || Number(craft?.craftId) === 0
```

迁移后需要改成：

```js
craft?.affix || isUniversalCraftBenchCraft(craft) || Number(craft?.craftId) === 0
```

否则这两个工艺在进入缓存前就被丢掉了，后续所有选择器都不可能显示。

### 3. 工艺标签生成要支持无 affix 工艺

原来的 `formatCraftBenchLabel` 主要围绕 `craft.affix` 拼装前后缀文本。

对于无 `affix` 的特殊工艺，需要保留这些兜底顺序：

```js
if (craft.text) return String(craft.text);
if (craft.label && craft.label !== '未选择工艺') return String(craft.label);
if (craft.name) return String(craft.name);
if (craft.description) return String(craft.description);
```

否则就算工艺进入缓存，选择器里也可能显示成：

- `未选择工艺`

### 4. 工艺词缀选择器要允许这类工艺进入所有装备类型

当前 `filterCraftBenchAffixesForPicker` 原本只接受：

```js
craft?.affix && Number(craft.affix.type || 0) === affixType
```

迁移后要改成：

```js
.filter((craft) => (
  isUniversalCraftBenchCraft(craft)
  || (craft?.affix && Number(craft.affix.type || 0) === affixType)
))
```

这样即使没有 `affix.type`，也能进入当前装备的工艺词缀下拉。

### 5. 装备类型匹配仍要保留

虽然这两个工艺是“无 affix 特殊工艺”，但仍然要走：

- `isCraftBenchForMask`

原因是 `/api/craft/list` 本身已经给了非常完整的 `equipmentTypes`。

当前本地还补了中文装备类型到掩码的兼容解析：

- `getCraftEquipmentTypeMask`
- `CRAFT_EQUIPMENT_TYPE_LABEL_MASKS`

如果下次原作者把 `equipmentTypes` 从纯数字改成中文或混合格式，这块兼容逻辑也要保留。

## 验证清单

迁移后建议至少验证以下场景：

- 任意装备类型进入词缀条件选择器后，`工艺词缀` 仍会出现。
- 展开 `工艺词缀` 详细列表后，能看到：
- `随机重掷稀有物品上的 3 条词缀`
- `随机重掷稀有物品上的 1 条词缀`
- 这两个工艺不会显示成 `未选择工艺`
- 普通带 `affix` 的工艺词缀仍然正常显示
- `移除工艺词缀` 这类特殊工艺逻辑不受影响
- 语法检查通过

## 关联补丁：Roll 判断自动排除破裂词缀

这条不是“特殊工艺进入选择器”的同一段代码，但和装备打造判断强相关，后续回迁时建议一起检查。

当前行为：

- 所有 Roll 条件判断都会先排除破裂词缀
- 再按剩余可变动词缀计算平均值或最低值

影响范围：

- `前缀平均Roll`
- `前缀最低Roll`
- `后缀平均Roll`
- `后缀最低Roll`
- `前后缀平均Roll`
- `前后缀最低Roll`
- `工艺词缀平均Roll`
- `工艺词缀最低Roll`

主要代码锚点：

- `isFracturedAffix`
- `AFFIX_ROLL_METRIC_SOURCES`
- `getAffixRollConditionValue`

推荐检索命令：

```powershell
rg -n "isFracturedAffix|AFFIX_ROLL_METRIC_SOURCES|getAffixRollConditionValue|craftedRollAverage|affixRollAverage" "idlepoe-assistant.user.js"
```

迁移要求：

在 `getAffixRollConditionValue` 中保留这条过滤：

```js
const affixes = (Array.isArray(equipment?.affixes) ? equipment.affixes : [])
  .filter((affix) => source.affixTypes.includes(Number(affix?.type)))
  .filter((affix) => !isFracturedAffix(affix))
  .filter((affix) => !source.craftedOnly || affix?.isCrafted === true);
```

不要回退成只按前后缀类型判断，否则会重新把神圣无法改变的破裂词缀计入 Roll 结果。

当前错误提示也已带上说明：

```js
throw new Error(`${equipment?.name || '当前装备'} 没有可计算“${metricLabel}”的${targetText}（已自动排除破裂词缀），已停止自动化打造。`);
```

这样用户在“全部词缀都破裂”或“只剩破裂词缀可参与该组判断”时，能直接看懂为什么停止。

验证建议：

- 4 条前后缀里有 1 条非满 Roll 破裂词缀时，`前后缀平均Roll >= 100%` 只按剩余 3 条计算
- 若某类 Roll 判断只剩破裂词缀，则应提示“已自动排除破裂词缀”
- 语法检查通过

## 语法检查

```powershell
node -e "const fs=require('fs'); const vm=require('vm'); new vm.Script(fs.readFileSync('idlepoe-assistant.user.js','utf8'), { filename: 'idlepoe-assistant.user.js' }); console.log('syntax ok')"
```
