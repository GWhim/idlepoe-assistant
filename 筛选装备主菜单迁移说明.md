# 筛选装备主菜单迁移说明

本文档用于在原作者后续更新测试服助手脚本后，重新迁移“筛选装备”主菜单及其后续增强功能。

当前参考文件：`idlepoe-assistant.user.js`

适用迁移范围：

- 从 `Add equipment filter menu` 开始。
- 包含后续“反向词缀筛选”“筛选结果批量通货/丢弃”“多条件组筛选”“查看详情兜底”“筛选日志提示”“单条详细词缀自动选中”等增强。

## 对应提交脉络

按当前仓库历史，这一功能链主要对应以下提交：

- `2dc7ea3` `Add equipment filter menu`
- `e47828b` `Add inverse affix equipment filter`
- `5ce2860` `Add actions for equipment filter results`
- `85a0160` `Support grouped affix conditions in equipment filter`
- `e1991ff` `Add fallback equipment detail modal`

当前文档还额外覆盖两处后续本地增强：

- 初筛完成后增加一条“正在逐件请求详细数据”的日志。
- 详细词缀列表只有 1 条时自动默认选中。

## 目标功能

迁移完成后，“筛选装备”主菜单应具备以下能力：

- 扫描背包或储藏装备。
- 支持关键词、装备类型、是否腐化、腐化基底筛选。
- 支持按词缀条件组筛选。
- 支持“满足任一条件组 / 不满足任一条件组 / 不按条件组筛选”。
- 支持对筛选结果批量使用通货。
- 支持批量丢弃筛选结果。
- 支持点击“查看详情”查看装备详情。
- 当网页原生详情模块失效时，自动回退到助手自己的详情弹窗。

## 主要代码锚点

下次迁移时，优先搜索这些函数和字段：

- `createEquipmentFilterSection`
- `runEquipmentFilter`
- `readEquipmentFilterOptions`
- `isEquipmentFilterSummaryMatched`
- `isEquipmentFilterDetailMatched`
- `renderEquipmentFilterResults`
- `applyCurrencyToEquipmentFilterResults`
- `destroyEquipmentFilterResults`
- `buildEquipmentFilterAffixConditions`
- `renderEquipmentFilterConditionBuilder`
- `refreshEquipmentFilterAffixTierSelect`
- `openEquipmentDetailModal`
- `openAssistantEquipmentDetailModal`

## 数据与状态

筛选装备依赖以下状态字段：

- `state.equipmentFilterResults`
- `state.equipmentFilterConditionGroups`
- `state.ui.equipmentFilterLocationSelect`
- `state.ui.equipmentFilterKeywordInput`
- `state.ui.equipmentFilterEquipmentSelect`
- `state.ui.equipmentFilterCorruptedSelect`
- `state.ui.equipmentFilterCorruptedBaseSelect`
- `state.ui.equipmentFilterAffixMatchSelect`
- `state.ui.equipmentFilterPositionSelect`
- `state.ui.equipmentFilterAffixTypeSelect`
- `state.ui.equipmentFilterAffixTierSelect`
- `state.ui.equipmentFilterAffixGroupSelect`
- `state.ui.equipmentFilterAffixGroupList`
- `state.ui.equipmentFilterBatchStoneSelect`
- `state.ui.equipmentFilterSummary`
- `state.ui.equipmentFilterResultList`

## 主菜单结构

`createEquipmentFilterSection` 负责组装整个筛选菜单，至少应保留这些控件：

- 读取位置：背包 / 储藏
- 关键词
- 装备类型
- 是否腐化
- 腐化基底
- 词缀条件
- 词缀位置
- 词缀类型
- 详细词缀
- 当前条件组
- 结果通货

以及这些按钮：

- `开始筛选`
- `对结果使用通货`
- `丢弃筛选结果`
- `停止`
- `清空结果`
- `添加到当前组`
- `添加条件组`
- `清空条件`

## 筛选逻辑

### 1. 初筛

`runEquipmentFilter` 先走分页接口读取列表页，只根据摘要信息做初筛：

- 装备类型
- 是否腐化
- 关键词

初筛阶段会更新摘要：

```js
setEquipmentFilterSummary(`正在扫描 ${getEquipmentFilterLocationLabel(options.location)}：第 ${page}/${totalPages} 页，初筛 ${summaryCandidates.length} 件。`);
```

### 2. 详细数据补查

如果存在以下任一条件，则需要逐件读取详情接口：

- `options.affixConditionGroups.length > 0`
- `options.corruptedBase !== 'any'`

此时需要保留这条日志，避免用户误以为脚本卡住：

```js
addLog(`正在对初筛出的 ${summaryCandidates.length} 件装备逐件请求详细数据。`, 'compact');
```

### 3. 详情筛选

详情筛选主要在 `isEquipmentFilterDetailMatched`：

- 腐化基底筛选
- 词缀条件组匹配
- 反向词缀筛选

词缀匹配依赖：

```js
const matched = isAffixMatched(equipment, options.affixConditionGroups);
```

并根据 `affixMatchMode` 决定：

- `include`：满足任一条件组
- `exclude`：不满足任一条件组
- `any`：不按条件组筛选

## 条件组迁移要点

当前“筛选装备”不再是单套“位置/类型/详细词缀”判断，而是独立条件组模型。

需要保留这些函数：

- `getEquipmentFilterAffixConditionGroupLabel`
- `getActiveEquipmentFilterAffixGroupIndex`
- `setEquipmentFilterAffixGroupMinRequired`
- `renderEquipmentFilterConditionBuilder`
- `addEquipmentFilterAffixConditions`
- `addEquipmentFilterAffixGroup`
- `removeEquipmentFilterAffixCondition`
- `removeEquipmentFilterAffixGroup`
- `clearEquipmentFilterAffixConditions`

逻辑要求：

- 组之间是“或”。
- 每组内可放多个条件。
- 每组有自己的“本组命中数”。
- 最终保存时用 `snapshotAffixConditionGroups(state.equipmentFilterConditionGroups)`。

## 结果操作迁移要点

### 批量通货

`applyCurrencyToEquipmentFilterResults` 负责对当前筛选结果批量处理：

- 只处理当前 `state.equipmentFilterResults`
- 支持普通通货
- 品质通货复用 `processQualityUntilMax`
- 其它通货复用 `processSingleStone`

### 批量丢弃

`destroyEquipmentFilterResults` 负责批量丢弃：

- 操作前弹确认框
- 成功丢弃后从 `state.equipmentFilterResults` 中移除
- 逐件更新列表和摘要

## 查看详情迁移要点

当前查看详情采用“双层方案”：

### 第一层：网页原生详情

优先调用：

- `loadPageEquipmentModalModules`
- `openPageEquipmentDetailModal`

并从网页资源里动态解析：

- `Equipment` 组件
- `createVNode`
- `Modal`

### 第二层：助手兜底详情

若网页原生详情模块不可用，则自动回退到：

- `openAssistantEquipmentDetailModal`
- `openEquipmentDetailModal`

兜底详情至少展示：

- 装备摘要
- 词缀列表
- 原始 JSON 数据

迁移时不要只保留原生详情逻辑，否则网页切版后会再次出现“查看详情没反应 / 网页端装备详情模块不可用”。

## 交互增强要点

### 详细词缀只有 1 条时自动选中

这条增强目前同时存在于：

- `refreshAffixTierSelect`
- `refreshEquipmentFilterAffixTierSelect`

保留逻辑：

```js
if (state.ui.equipmentFilterAffixTierSelect.options.length === 1) {
  state.ui.equipmentFilterAffixTierSelect.options[0].selected = true;
}
```

主词缀判断区也有同样逻辑：

```js
if (state.ui.affixTierSelect.options.length === 1) {
  state.ui.affixTierSelect.options[0].selected = true;
}
```

## 推荐检索命令

```powershell
rg -n "createEquipmentFilterSection|runEquipmentFilter|equipmentFilterConditionGroups|applyCurrencyToEquipmentFilterResults|destroyEquipmentFilterResults|openEquipmentDetailModal|openAssistantEquipmentDetailModal|正在对初筛出的" "idlepoe-assistant.user.js"
```

## 验证清单

迁移后建议至少验证以下场景：

- 能正常打开 `筛选装备` 主菜单。
- 背包 / 储藏筛选都能跑通。
- 初筛后如果需要详情补查，会出现“正在对初筛出的 X 件装备逐件请求详细数据”日志。
- 腐化基底筛选正常。
- `满足任一条件组` 与 `不满足任一条件组` 都正常。
- 可新增多个条件组，每组可设置命中数。
- 详细词缀只有 1 条时会自动默认选中。
- `对结果使用通货` 正常。
- `丢弃筛选结果` 正常。
- `查看详情` 在网页原生模块正常时可打开原生详情。
- 若网页原生详情失效，也能自动打开助手兜底详情。
- 语法检查通过。

## 语法检查

```powershell
node -e "const fs=require('fs'); const vm=require('vm'); new vm.Script(fs.readFileSync('idlepoe-assistant.user.js','utf8'), { filename: 'idlepoe-assistant.user.js' }); console.log('syntax ok')"
```
