# 手机端多选横向滚动迁移说明

本文档用于在测试服助手脚本更新后，重新补回“手机端多选展示区域可横向滚动查看完整文字”的样式补丁。

当前参考文件：`idlepoe-assistant.user.js`

## 2.22.1 迁移记录

本次以原作者 `测试服助手2.22.1.txt` 为基地，只补回多选区域横向滚动样式：

- 保留原版文字，不改文案内容。
- 保持单行展示，不改成换行。
- 去掉多选展示区域的省略号截断效果。
- 在手机端可横向拖动查看完整超长文字。
- 不修改 `slotLabel`、技能石来源标签、业务逻辑或接口流程。

## 目标功能

多选展示区域在手机窄屏上经常出现“文字超出后看不到”的问题。本补丁目标是让这些区域保留原版长文本，同时允许横向滚动查看完整内容。

重点覆盖：

- 原生 `select.poe2-input[multiple]`。
- 通用自定义多选摘要 `.poe2-multiselect-summary-text`。
- 通用自定义多选已选摘要 `.poe2-multiselect-selected` / `.poe2-multiselect-summary-line`。
- 通用自定义多选选项列表 `.poe2-multiselect-options` / `.poe2-multiselect-option`。

## 代码锚点

下次迁移时优先搜索这些 CSS 选择器：

- `select.poe2-input[multiple]`
- `select.poe2-input[multiple] option`
- `.poe2-multiselect-trigger`
- `.poe2-multiselect-summary-text`
- `.poe2-multiselect-selected`
- `.poe2-multiselect-summary-line`
- `.poe2-multiselect-options`
- `.poe2-multiselect-option`
- `.poe2-multiselect-option span`

当前脚本中这些样式位于 `installStyles` 的 `GM_addStyle` 模板字符串内。行号会随原作者版本变化，迁移时以选择器为准。

## 关键样式

原生多选框需要允许横向滚动，并保持选项单行：

```css
select.poe2-input[multiple] {
  height: 96px;
  overflow: auto;
  white-space: nowrap;
  -webkit-overflow-scrolling: touch;
}

select.poe2-input[multiple] option {
  white-space: nowrap;
}
```

自定义多选触发器摘要不要省略，要把可用宽度留给摘要文字横向滚动：

```css
.poe2-multiselect-trigger {
  overflow: hidden;
}

.poe2-multiselect-summary-text {
  display: block;
  flex: 1 1 auto;
  min-width: 0;
  overflow-x: auto;
  overflow-y: hidden;
  text-overflow: clip;
  white-space: nowrap;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: thin;
}
```

已选摘要区域要能横向拖动，摘要行本身保持完整宽度：

```css
.poe2-multiselect-selected {
  min-width: 0;
  overflow-x: auto;
  overflow-y: hidden;
  padding-bottom: 6px;
  border-bottom: 1px solid var(--poe2-border);
  white-space: nowrap;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: thin;
}

.poe2-multiselect-summary-line {
  display: block;
  width: max-content;
  min-width: 100%;
  overflow: visible;
  text-overflow: clip;
  white-space: nowrap;
}
```

选项列表仍然纵向滚动，但每一行允许撑出横向宽度，用户可以左右拖动列表查看完整文本：

```css
.poe2-multiselect-options {
  overflow: auto;
  -webkit-overflow-scrolling: touch;
}

.poe2-multiselect-option {
  grid-template-columns: 18px max-content;
  box-sizing: border-box;
  width: max-content;
  min-width: 100%;
  white-space: nowrap;
}

.poe2-multiselect-option span {
  white-space: nowrap;
}
```

## 不要迁移的内容

本补丁只解决横向滚动展示问题，下次迁移时不要顺手改这些内容：

- 不要把长文本改成换行展示。
- 不要缩短、多段重写或替换原作者文案。
- 不要修改 `formatSkillStoneLabel`。
- 不要修改 `slotLabel` 或装备槽位显示逻辑。
- 不要改动技能石取下、丢弃、升级、棱镜、腐化等业务流程。

## 验证清单

迁移后建议检查以下场景：

- 手机端打开包含通用多选组件的区域，摘要文字超长时可左右拖动查看完整内容。
- 打开多选下拉面板，已选摘要超长时可左右拖动查看完整内容。
- 多选选项文字超长时保持单行，列表区域可横向拖动。
- 原生 `select multiple` 中超长选项保持单行，区域可横向滚动。
- 文本不出现省略号截断。
- 语法检查通过。

## 推荐检索命令

```powershell
rg -n "select\\.poe2-input\\[multiple\\]|poe2-multiselect-summary-text|poe2-multiselect-selected|poe2-multiselect-summary-line|poe2-multiselect-options|poe2-multiselect-option" "idlepoe-assistant.user.js"
```

语法检查：

```powershell
node -e "const fs=require('fs'); const vm=require('vm'); new vm.Script(fs.readFileSync('idlepoe-assistant.user.js','utf8'), { filename: 'idlepoe-assistant.user.js' }); console.log('syntax ok')"
```
